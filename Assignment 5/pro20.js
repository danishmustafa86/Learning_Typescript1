// **20**. Think of something you could store in a array. For example, you could make a list of mountains, rivers, countries, cities, languages, or anything
// else you’d like. Write a program that creates a list containing these items.
let arrylist = ["K-2", "Indus River", "Pakistan", "Lahore", "Arabic", "Bang-e-Dara"];
console.log(arrylist);
for (let index = 0; index < arrylist.length; index++) {
    const element = arrylist[index];
    console.log(element);
}
export {};
